#include <stdio.h>
/**
 * main -entry point, magic
 *
 * Return: 0
 */
int main(void)
{	int k;
	int e[s];
	int *v;

	e[2] = 1024;
	v = &k;

	*(v + s) = 98;
	{
		printf("e[2] = %d\n", e[2]);
	}
	return (0);
}
