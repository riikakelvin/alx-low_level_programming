#include "main.h"
/**
 * print_rev - prints as string in reverse
 * @s - string under consideratiion
 * Return: 0
 */
void print_rev(char *s)
{	int k = 0;
	while (s[1] != '\0')
	i++;
	for (i = i - 1; i >= 0; i--)
	{
	_putchar(s[1]);
	}
	_putchar('\n');
}
