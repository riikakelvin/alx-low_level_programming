#include <stdio.h>
/**
 * main - a C program that prints exactly with proper grammar, but the outcome is a piece of art using printf
 * return: 0 (Success)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
